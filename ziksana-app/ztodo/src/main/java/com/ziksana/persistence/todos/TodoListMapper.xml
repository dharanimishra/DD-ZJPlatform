<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ziksana.persistence.todos.TodoListMapper">

	<select id="findTodo" resultMap="todoResultMap" parameterType="String">
		SELECT * FROM memMemberToDo WHERE ID = #{id}
	</select>

	<resultMap id="todoResultMap" type="com.ziksana.domain.todos.TodoList">
		<result property="id" column="ID" />
		<result property="category" column="category" />
		<result property="description" column="description" />
		<result property="complete" column="complete" />
		<result property="memberRoleId" column="memberRoleId" />
		<result property="creationDate" column="creationDate" />
	</resultMap>


	<insert id="createTodoList" parameterType="com.ziksana.domain.todos.TodoList"
		useGeneratedKeys="true" keyProperty="id">
		INSERT INTO
		memMemberToDo(ID,category,description,complete,memberRoleId,createDate)
		VALUES
		(#{id},#{category},#{description},#{complete},#{memberRoleId},#{creationDate})
	</insert>


	<update id="updateTodoList" parameterType="com.ziksana.domain.todos.TodoList">
		UPDATE memMemberToDo SET
		creationDate= #{creationDate},category=category#{category} WHERE id =
		#{id}
	</update>

	<delete id="deleteTodoList" parameterType="String">
		DELETE FROM memMemberToDo
		WHERE ID = #{id}
	</delete>
</mapper>
